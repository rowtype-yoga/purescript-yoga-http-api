{
  "name": "purescript-yoga-http-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spago build",
    "test": "bun run build && bun run vitest run",
    "test:watch": "bun run vitest"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "purescript": "^0.15.15",
    "spago": "^1.0.3",
    "vitest": "^1.0.0"
  }
}
